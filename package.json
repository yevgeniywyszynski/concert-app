{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "4.17.1",
    "nodemon": "^2.0.15",
    "uuidv4": "^6.2.12"
  },
  "scripts": {
    "build": "cd client && yarn install && yarn build"
  }
}
